{% extends 'base.html.twig' %}

{% block body %}
	<h1>Modifier un utilisateur</h1>
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
	<a href="{{ path('admin_users') }}">Retour</a>
{% endblock %}
    